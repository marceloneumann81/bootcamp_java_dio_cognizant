#DEV environment

app:
  message: This is property file to the${spring.application.name} especific to DEV Environment

spring:
  driver-class-name: org.h2.driver
  url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
  username: sa
  password: sa